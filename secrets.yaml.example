# Exemple de fichier secrets.yaml pour nixos-kid
#
# PRÉREQUIS: Configurer .sops.yaml avec DEUX clés:
#
# 1. CLÉ SSH DE L'HÔTE NIXOS (pour déchiffrage au boot):
#    Sur la machine NixOS:
#      ssh-to-age < /etc/ssh/ssh_host_ed25519_key.pub
#    Copier le résultat dans .sops.yaml (YOUR_SSH_HOST_KEY)
#
# 2. CLÉ AGE DE L'ADMIN (pour éditer depuis votre poste):
#    Sur votre poste de travail:
#      mkdir -p ~/.config/sops/age
#      age-keygen -o ~/.config/sops/age/keys.txt
#      age-keygen -y ~/.config/sops/age/keys.txt  # Affiche la clé publique
#    Copier le résultat dans .sops.yaml (YOUR_ADMIN_AGE_KEY)
#
# UTILISATION:
#
# 1. Copier ce fichier:
#    cp secrets.yaml.example secrets.yaml
#
# 2. Éditer avec sops (depuis votre poste admin):
#    sops secrets.yaml
#
# 3. Remplacer les valeurs par vos vraies valeurs
#
# Pour générer le hash bcrypt AdGuard Home:
#    htpasswd -nbB admin VotreMotDePasse | cut -d: -f2
#    OU
#    echo -n 'VotreMotDePasse' | mkpasswd -m bcrypt -s

# Hash bcrypt du mot de passe admin AdGuard Home
adguard-admin-password: $2y$10$REPLACE_WITH_ACTUAL_HASH

# Mot de passe initial pour l'utilisateur enfant
child-initial-password: changeme

# Nom d'utilisateur de l'enfant
child-username: enfant

# Nom complet de l'enfant
child-fullname: Mon Enfant
